const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migration'), //sequelize cria pasta
  'seeders-path': resolve(__dirname, 'src', 'database', 'seed'), //sequelize cria pasta
};
